{
  "name": "broodkeepers-oath",
  "version": "0.1.0",
  "private": true,
  "description": "The Broodkeeper's Oath — a monster-taming, breeding and sanctuary RPG for mobile browsers.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:browser": "node tests/browser/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
